# AuditLog chain

## Local
1) npm i
2) npm run node
3) (new terminal) npm run compile
4) (new terminal) npm run deploy:local
Copy the deployed contract address into server/.env as AUDIT_CONTRACT_ADDRESS